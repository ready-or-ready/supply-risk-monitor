# ── 데이터 수집 ──────────────────────────────────────────
# 관세청 GW, 외교부 안전공지, 한국은행 ECOS, 중기부 지원사업 공고 API
requests>=2.32.5
# 연합뉴스 RSS SSL 우회 어댑터 (LegacyTLSAdapter) — requests 의존성이지만 직접 사용
urllib3>=2.7.0

# ── 데이터 처리 ──────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0

# ── 시장 데이터 ──────────────────────────────────────────
# Brent(BZ=F) · WTI(CL=F) · USDKRW · 대한유화(006650.KS) · KOSPI(^KS11)
yfinance>=0.2.40

# ── 웹 프론트엔드 ─────────────────────────────────────────
streamlit>=1.35.0

# ── 인터랙티브 차트 ───────────────────────────────────────
plotly>=5.0.0

# ── 리포트 다운로드 ───────────────────────────────────────
# 리포트 생성 페이지 xlsx 다운로드 (Section 1~6 결과 내보내기)
openpyxl>=3.1.0

# ── LLM 분석 ─────────────────────────────────────────────
# AI 종합 분석 (claude-haiku-4-5) — ANTHROPIC_KEY 미설정 시 하드코딩 fallback
openai>=1.0.0
